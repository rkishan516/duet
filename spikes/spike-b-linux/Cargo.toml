[package]
name = "spike-b-linux"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
tao = "0.36.0"
wry = "0.56.0"
# The same gtk-rs stack tao 0.36 itself links, so every widget handle this
# spike passes around belongs to one GTK.
gtk = "0.18"
glib = "0.18"
glib-sys = "0.18"
gobject-sys = "0.18"
