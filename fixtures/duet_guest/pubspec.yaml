name: duet_guest
description: >-
  The Dart guest client for duet-protocol: a tracked fixture, not a published
  package. See README.md for what it is and how to build it.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0-133.0.dev

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
