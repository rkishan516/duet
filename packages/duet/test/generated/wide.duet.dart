// GENERATED BY duet-codegen. DO NOT EDIT BY HAND.
//
// Source:     schema/wide.json
// Regenerate: cargo test -p duet-codegen --test goldens -- --ignored regenerate_goldens
//
// Every path below is a string literal minted and validated when this
// file was generated. Nothing here builds a path out of a runtime
// value, and every path segment is the schema's own wire key, never a
// camel-cased one — the accessor names are camel-cased and the paths
// are not, deliberately, because two guests that disagree about a wire
// key silently stop seeing each other's writes.
//
// There are no algorithms here. Every decode, every merge and every
// push route lives in the hand-written Dart runtime this file
// delegates to.

/// The typed Duet client generated from `schema/wide.json`.
library;

import 'package:duet/duet.dart';
import 'package:duet/typed.dart';

/// `Editor`, as the schema describes it.
final class Editor {
  /// Creates an `Editor`.
  const Editor({required this.zoom, required this.theme});

  /// The `zoom` field.
  final double zoom;

  /// The `theme` field.
  final String theme;

  @override
  bool operator ==(Object other) =>
      other is Editor &&
      other.zoom == zoom &&
      other.theme == theme;

  @override
  int get hashCode => Object.hashAll(<Object?>[zoom, theme]);

  @override
  String toString() =>
      'Editor(zoom: $zoom, theme: $theme)';
}

/// The [DuetCodec] for [Editor].
final class EditorCodec implements DuetCodec<Editor> {
  /// Creates the codec.
  const EditorCodec();

  @override
  String get name => 'Editor';

  @override
  DuetValue encode(Editor value) {
    return DuetMap(<String, DuetValue>{
      'zoom': duetFloatCodec.encode(value.zoom),
      'theme': duetStringCodec.encode(value.theme),
    });
  }

  @override
  Editor? decode(DuetValue value) {
    if (value is! DuetMap) return null;
    final DuetReading<double> zoom =
        duetRequiredReading<double>(duetFloatCodec, value.entries['zoom']);
    if (zoom is! DuetPresent<double>) return null;
    final DuetReading<String> theme =
        duetRequiredReading<String>(duetStringCodec, value.entries['theme']);
    if (theme is! DuetPresent<String>) return null;
    return Editor(zoom: zoom.value, theme: theme.value);
  }
}

/// `Outer`, as the schema describes it.
final class Outer {
  /// Creates an `Outer`.
  const Outer({required this.inner, required this.depth});

  /// The `inner` field.
  final Editor inner;

  /// The `depth` field.
  final int depth;

  @override
  bool operator ==(Object other) =>
      other is Outer &&
      other.inner == inner &&
      other.depth == depth;

  @override
  int get hashCode => Object.hashAll(<Object?>[inner, depth]);

  @override
  String toString() =>
      'Outer(inner: $inner, depth: $depth)';
}

/// The [DuetCodec] for [Outer].
final class OuterCodec implements DuetCodec<Outer> {
  /// Creates the codec.
  const OuterCodec();

  @override
  String get name => 'Outer';

  @override
  DuetValue encode(Outer value) {
    return DuetMap(<String, DuetValue>{
      'inner': const EditorCodec().encode(value.inner),
      'depth': duetIntCodec.encode(value.depth),
    });
  }

  @override
  Outer? decode(DuetValue value) {
    if (value is! DuetMap) return null;
    final DuetReading<Editor> inner =
        duetRequiredReading<Editor>(const EditorCodec(), value.entries['inner']);
    if (inner is! DuetPresent<Editor>) return null;
    final DuetReading<int> depth =
        duetRequiredReading<int>(duetIntCodec, value.entries['depth']);
    if (depth is! DuetPresent<int>) return null;
    return Outer(inner: inner.value, depth: depth.value);
  }
}

/// `Wide`, as the schema describes it.
final class Wide {
  /// Creates a `Wide`.
  const Wide({
    required this.flag,
    required this.count,
    required this.ratio,
    required this.label,
    required this.blob,
    required this.anything,
    required this.maybeLabel,
    required this.maybeRatios,
    required this.maybeEditor,
    required this.tags,
    required this.matrix,
    required this.lookup,
    required this.editors,
    required this.blobs,
    required this.loose,
    required this.flags,
    required this.outer,
    required this.snakeCaseField,
  });

  /// The `flag` field.
  final bool flag;

  /// The `count` field.
  final int count;

  /// The `ratio` field.
  final double ratio;

  /// The `label` field.
  final String label;

  /// The `blob` field.
  final List<int> blob;

  /// The `anything` field.
  final DuetValue anything;

  /// The `maybe_label` field.
  final String? maybeLabel;

  /// The `maybe_ratios` field.
  final List<double>? maybeRatios;

  /// The `maybe_editor` field.
  final Editor? maybeEditor;

  /// The `tags` field.
  final List<String> tags;

  /// The `matrix` field.
  final List<List<int>> matrix;

  /// The `lookup` field.
  final Map<String, int> lookup;

  /// The `editors` field.
  final Map<String, Editor> editors;

  /// The `blobs` field.
  final List<List<int>> blobs;

  /// The `loose` field.
  final Map<String, DuetValue> loose;

  /// The `flags` field.
  final List<bool> flags;

  /// The `outer` field.
  final Outer outer;

  /// The `snake_case_field` field.
  final String snakeCaseField;

  @override
  bool operator ==(Object other) =>
      other is Wide &&
      other.flag == flag &&
      other.count == count &&
      other.ratio == ratio &&
      other.label == label &&
      other.blob == blob &&
      other.anything == anything &&
      other.maybeLabel == maybeLabel &&
      other.maybeRatios == maybeRatios &&
      other.maybeEditor == maybeEditor &&
      other.tags == tags &&
      other.matrix == matrix &&
      other.lookup == lookup &&
      other.editors == editors &&
      other.blobs == blobs &&
      other.loose == loose &&
      other.flags == flags &&
      other.outer == outer &&
      other.snakeCaseField == snakeCaseField;

  @override
  int get hashCode => Object.hashAll(<Object?>[
    flag,
    count,
    ratio,
    label,
    blob,
    anything,
    maybeLabel,
    maybeRatios,
    maybeEditor,
    tags,
    matrix,
    lookup,
    editors,
    blobs,
    loose,
    flags,
    outer,
    snakeCaseField,
  ]);

  @override
  String toString() =>
      'Wide(flag: $flag, count: $count, ratio: $ratio, label: $label, blob: $blob, anything: $anything, maybeLabel: $maybeLabel, maybeRatios: $maybeRatios, maybeEditor: $maybeEditor, tags: $tags, matrix: $matrix, lookup: $lookup, editors: $editors, blobs: $blobs, loose: $loose, flags: $flags, outer: $outer, snakeCaseField: $snakeCaseField)';
}

/// The [DuetCodec] for [Wide].
final class WideCodec implements DuetCodec<Wide> {
  /// Creates the codec.
  const WideCodec();

  @override
  String get name => 'Wide';

  @override
  DuetValue encode(Wide value) {
    final String? maybeLabel = value.maybeLabel;
    final List<double>? maybeRatios = value.maybeRatios;
    final Editor? maybeEditor = value.maybeEditor;
    return DuetMap(<String, DuetValue>{
      'flag': duetBoolCodec.encode(value.flag),
      'count': duetIntCodec.encode(value.count),
      'ratio': duetFloatCodec.encode(value.ratio),
      'label': duetStringCodec.encode(value.label),
      'blob': duetBytesCodec.encode(value.blob),
      'anything': duetDynamicCodec.encode(value.anything),
      'maybe_label': maybeLabel == null ? const DuetNull() : duetStringCodec.encode(maybeLabel),
      'maybe_ratios': maybeRatios == null ? const DuetNull() : duetListCodec<double>(duetFloatCodec).encode(maybeRatios),
      'maybe_editor': maybeEditor == null ? const DuetNull() : const EditorCodec().encode(maybeEditor),
      'tags': duetListCodec<String>(duetStringCodec).encode(value.tags),
      'matrix': duetListCodec<List<int>>(duetListCodec<int>(duetIntCodec)).encode(value.matrix),
      'lookup': duetMapCodec<int>(duetIntCodec).encode(value.lookup),
      'editors': duetMapCodec<Editor>(const EditorCodec()).encode(value.editors),
      'blobs': duetListCodec<List<int>>(duetBytesCodec).encode(value.blobs),
      'loose': duetMapCodec<DuetValue>(duetDynamicCodec).encode(value.loose),
      'flags': duetListCodec<bool>(duetBoolCodec).encode(value.flags),
      'outer': const OuterCodec().encode(value.outer),
      'snake_case_field': duetStringCodec.encode(value.snakeCaseField),
    });
  }

  @override
  Wide? decode(DuetValue value) {
    if (value is! DuetMap) return null;
    final DuetReading<bool> flag =
        duetRequiredReading<bool>(duetBoolCodec, value.entries['flag']);
    if (flag is! DuetPresent<bool>) return null;
    final DuetReading<int> count =
        duetRequiredReading<int>(duetIntCodec, value.entries['count']);
    if (count is! DuetPresent<int>) return null;
    final DuetReading<double> ratio =
        duetRequiredReading<double>(duetFloatCodec, value.entries['ratio']);
    if (ratio is! DuetPresent<double>) return null;
    final DuetReading<String> label =
        duetRequiredReading<String>(duetStringCodec, value.entries['label']);
    if (label is! DuetPresent<String>) return null;
    final DuetReading<List<int>> blob =
        duetRequiredReading<List<int>>(duetBytesCodec, value.entries['blob']);
    if (blob is! DuetPresent<List<int>>) return null;
    final DuetReading<DuetValue> anything =
        duetRequiredReading<DuetValue>(duetDynamicCodec, value.entries['anything']);
    if (anything is! DuetPresent<DuetValue>) return null;
    final DuetReading<String> maybeLabel =
        duetOptionalReading<String>(duetStringCodec, value.entries['maybe_label']);
    if (maybeLabel is DuetAbsent<String> || maybeLabel is DuetMismatch<String>) return null;
    final DuetReading<List<double>> maybeRatios =
        duetOptionalReading<List<double>>(duetListCodec<double>(duetFloatCodec), value.entries['maybe_ratios']);
    if (maybeRatios is DuetAbsent<List<double>> || maybeRatios is DuetMismatch<List<double>>) return null;
    final DuetReading<Editor> maybeEditor =
        duetOptionalReading<Editor>(const EditorCodec(), value.entries['maybe_editor']);
    if (maybeEditor is DuetAbsent<Editor> || maybeEditor is DuetMismatch<Editor>) return null;
    final DuetReading<List<String>> tags =
        duetRequiredReading<List<String>>(duetListCodec<String>(duetStringCodec), value.entries['tags']);
    if (tags is! DuetPresent<List<String>>) return null;
    final DuetReading<List<List<int>>> matrix =
        duetRequiredReading<List<List<int>>>(duetListCodec<List<int>>(duetListCodec<int>(duetIntCodec)), value.entries['matrix']);
    if (matrix is! DuetPresent<List<List<int>>>) return null;
    final DuetReading<Map<String, int>> lookup =
        duetRequiredReading<Map<String, int>>(duetMapCodec<int>(duetIntCodec), value.entries['lookup']);
    if (lookup is! DuetPresent<Map<String, int>>) return null;
    final DuetReading<Map<String, Editor>> editors =
        duetRequiredReading<Map<String, Editor>>(duetMapCodec<Editor>(const EditorCodec()), value.entries['editors']);
    if (editors is! DuetPresent<Map<String, Editor>>) return null;
    final DuetReading<List<List<int>>> blobs =
        duetRequiredReading<List<List<int>>>(duetListCodec<List<int>>(duetBytesCodec), value.entries['blobs']);
    if (blobs is! DuetPresent<List<List<int>>>) return null;
    final DuetReading<Map<String, DuetValue>> loose =
        duetRequiredReading<Map<String, DuetValue>>(duetMapCodec<DuetValue>(duetDynamicCodec), value.entries['loose']);
    if (loose is! DuetPresent<Map<String, DuetValue>>) return null;
    final DuetReading<List<bool>> flags =
        duetRequiredReading<List<bool>>(duetListCodec<bool>(duetBoolCodec), value.entries['flags']);
    if (flags is! DuetPresent<List<bool>>) return null;
    final DuetReading<Outer> outer =
        duetRequiredReading<Outer>(const OuterCodec(), value.entries['outer']);
    if (outer is! DuetPresent<Outer>) return null;
    final DuetReading<String> snakeCaseField =
        duetRequiredReading<String>(duetStringCodec, value.entries['snake_case_field']);
    if (snakeCaseField is! DuetPresent<String>) return null;
    return Wide(
      flag: flag.value,
      count: count.value,
      ratio: ratio.value,
      label: label.value,
      blob: blob.value,
      anything: anything.value,
      maybeLabel: maybeLabel.valueOrNull,
      maybeRatios: maybeRatios.valueOrNull,
      maybeEditor: maybeEditor.valueOrNull,
      tags: tags.value,
      matrix: matrix.value,
      lookup: lookup.value,
      editors: editors.value,
      blobs: blobs.value,
      loose: loose.value,
      flags: flags.value,
      outer: outer.value,
      snakeCaseField: snakeCaseField.value,
    );
  }
}

/// Typed accessors for `Wide` at the store's root.
///
/// Every path here is a literal; see this file's header.
final class WideClient {
  /// Binds these accessors to [router].
  const WideClient(this.router);

  /// The router every accessor below is bound to.
  final DuetRouter router;

  /// `Wide` itself, as one value at the store's root.
  DuetField<Wide> get self =>
      DuetField<Wide>(router, '', const WideCodec());

  /// `Wide.flag` at `flag`.
  DuetField<bool> get flag =>
      DuetField<bool>(router, 'flag', duetBoolCodec);

  /// `Wide.count` at `count`.
  DuetField<int> get count =>
      DuetField<int>(router, 'count', duetIntCodec);

  /// `Wide.ratio` at `ratio`.
  DuetField<double> get ratio =>
      DuetField<double>(router, 'ratio', duetFloatCodec);

  /// `Wide.label` at `label`.
  DuetField<String> get label =>
      DuetField<String>(router, 'label', duetStringCodec);

  /// `Wide.blob` at `blob`.
  DuetField<List<int>> get blob =>
      DuetField<List<int>>(router, 'blob', duetBytesCodec);

  /// `Wide.anything` at `anything`.
  DuetField<DuetValue> get anything =>
      DuetField<DuetValue>(router, 'anything', duetDynamicCodec);

  /// `Wide.maybe_label` at `maybe_label`.
  DuetOptionalField<String> get maybeLabel =>
      DuetOptionalField<String>(router, 'maybe_label', duetStringCodec);

  /// `Wide.maybe_ratios` at `maybe_ratios`.
  DuetOptionalField<List<double>> get maybeRatios =>
      DuetOptionalField<List<double>>(router, 'maybe_ratios', duetListCodec<double>(duetFloatCodec));

  /// `Wide.maybe_editor`'s own accessors, at `maybe_editor`.
  WideMaybeEditorClient get maybeEditor => WideMaybeEditorClient(router);

  /// `Wide.tags` at `tags`.
  DuetField<List<String>> get tags =>
      DuetField<List<String>>(router, 'tags', duetListCodec<String>(duetStringCodec));

  /// `Wide.matrix` at `matrix`.
  DuetField<List<List<int>>> get matrix =>
      DuetField<List<List<int>>>(router, 'matrix', duetListCodec<List<int>>(duetListCodec<int>(duetIntCodec)));

  /// `Wide.lookup` at `lookup`.
  DuetField<Map<String, int>> get lookup =>
      DuetField<Map<String, int>>(router, 'lookup', duetMapCodec<int>(duetIntCodec));

  /// `Wide.editors` at `editors`.
  DuetField<Map<String, Editor>> get editors =>
      DuetField<Map<String, Editor>>(router, 'editors', duetMapCodec<Editor>(const EditorCodec()));

  /// `Wide.blobs` at `blobs`.
  DuetField<List<List<int>>> get blobs =>
      DuetField<List<List<int>>>(router, 'blobs', duetListCodec<List<int>>(duetBytesCodec));

  /// `Wide.loose` at `loose`.
  DuetField<Map<String, DuetValue>> get loose =>
      DuetField<Map<String, DuetValue>>(router, 'loose', duetMapCodec<DuetValue>(duetDynamicCodec));

  /// `Wide.flags` at `flags`.
  DuetField<List<bool>> get flags =>
      DuetField<List<bool>>(router, 'flags', duetListCodec<bool>(duetBoolCodec));

  /// `Wide.outer`'s own accessors, at `outer`.
  WideOuterClient get outer => WideOuterClient(router);

  /// `Wide.snake_case_field` at `snake_case_field`.
  DuetField<String> get snakeCaseField =>
      DuetField<String>(router, 'snake_case_field', duetStringCodec);
}

/// Typed accessors for `Editor` at `maybe_editor`.
///
/// Every path here is a literal; see this file's header.
final class WideMaybeEditorClient {
  /// Binds these accessors to [router].
  const WideMaybeEditorClient(this.router);

  /// The router every accessor below is bound to.
  final DuetRouter router;

  /// `Editor` itself, as one value at `maybe_editor`.
  DuetOptionalField<Editor> get self =>
      DuetOptionalField<Editor>(router, 'maybe_editor', const EditorCodec());

  /// `Editor.zoom` at `maybe_editor.zoom`.
  DuetField<double> get zoom =>
      DuetField<double>(router, 'maybe_editor.zoom', duetFloatCodec);

  /// `Editor.theme` at `maybe_editor.theme`.
  DuetField<String> get theme =>
      DuetField<String>(router, 'maybe_editor.theme', duetStringCodec);
}

/// Typed accessors for `Outer` at `outer`.
///
/// Every path here is a literal; see this file's header.
final class WideOuterClient {
  /// Binds these accessors to [router].
  const WideOuterClient(this.router);

  /// The router every accessor below is bound to.
  final DuetRouter router;

  /// `Outer` itself, as one value at `outer`.
  DuetField<Outer> get self =>
      DuetField<Outer>(router, 'outer', const OuterCodec());

  /// `Outer.inner`'s own accessors, at `outer.inner`.
  WideOuterInnerClient get inner => WideOuterInnerClient(router);

  /// `Outer.depth` at `outer.depth`.
  DuetField<int> get depth =>
      DuetField<int>(router, 'outer.depth', duetIntCodec);
}

/// Typed accessors for `Editor` at `outer.inner`.
///
/// Every path here is a literal; see this file's header.
final class WideOuterInnerClient {
  /// Binds these accessors to [router].
  const WideOuterInnerClient(this.router);

  /// The router every accessor below is bound to.
  final DuetRouter router;

  /// `Editor` itself, as one value at `outer.inner`.
  DuetField<Editor> get self =>
      DuetField<Editor>(router, 'outer.inner', const EditorCodec());

  /// `Editor.zoom` at `outer.inner.zoom`.
  DuetField<double> get zoom =>
      DuetField<double>(router, 'outer.inner.zoom', duetFloatCodec);

  /// `Editor.theme` at `outer.inner.theme`.
  DuetField<String> get theme =>
      DuetField<String>(router, 'outer.inner.theme', duetStringCodec);
}
